import treemap from "../assets/treemap.png";

const Treemap = () => {
  return (
    <div>
      <img
        src={treemap}
        alt="Line Graph"
        style={{ width: "100%", maxWidth: "600px" }}
      />
    </div>
  );
};

export default Treemap;
